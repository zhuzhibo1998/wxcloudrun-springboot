server:
  port: 80


spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${MYSQL_ADDRESS:sh-cynosdbmysql-grp-px5402la.sql.tencentcdb.com}:${DS_PORT:21822}/${MYSQL_DATABASE:springboot_demo}?serverTimezone=Asia/Shanghai&useSSL=false
    username: ${MYSQL_USERNAME:root}
    password: ${MySQL_PASSWORD:BN9CtYaX}
  mvc:
    view:
      suffix: .html
      prefix: /

mybatis:
  mapper-locations: classpath*:mapper/*Mapper.xml
